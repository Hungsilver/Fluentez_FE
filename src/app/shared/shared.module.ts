import { NgModule } from '@angular/core';
import { NotFoundComponent } from './components';

@NgModule({
  imports: [
  ],
  declarations: [
    NotFoundComponent,
  ]
})
export class SharedModule { }
